# Recess Website

## Environment Variables

Set the following environment variables for integrations:

- `PIPEDRIVE_API_TOKEN`: Pipedrive API token used by `/api/pipedrive`.
- `NEXT_PUBLIC_CUSTOMER_IO_WRITE_KEY`: Customer.io Pipelines write key used by the browser SDK.

For local development, add them to `.env.local`.
